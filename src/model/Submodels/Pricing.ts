class Pricing {
    price: number;
    currency: string;



    constructor(
        price: number,
        currency: string,
    ) {
        this.price = price;
        this.currency = currency;
    }
}
export default Pricing;